' ============================================================
' yield_table.xlsm — ThisWorkbook 에만 넣을 코드
' ============================================================
' 준비:
'   1) VBA 편집기에서 [파일] > [파일 가져오기] 로
'      YieldTable_AutoExport.bas 를 표준 모듈로 가져오기
'   2) 아래 Private Sub Workbook_Open 전체를 ThisWorkbook 에 붙여넣기
'      (기존 Workbook_Open 내용은 삭제)
' ============================================================

Option Explicit

Private Sub Workbook_Open()
    ExportYieldTableToCsvAndQuit
End Sub
